# Phase 5 Examples — Show

_Worked thread: the same user's **File Organizer**. The tool ran on the real `Posts` folder — images and videos sorted by date. Two files are saved in the project folder: `identity.md` and `context.md`._

---

## Step 0: The Offer

Coach: "Before we wrap — want to take two minutes to put what you built into a shareable package? It is optional; we can stop here if you would rather."

---

### Skip path (user declines)

User: "I am good, let's stop here."
Coach: "Great job!"

---

### Complete path (user says yes)

---

## Step 1: Draft the Description

Coach: "My Posts folder was a mess of images and videos, so I built a tool to fix that. It sorts any folder of media files into separate images and videos folders, naming each file by the date it was made. The tool is two markdown files — `identity.md` and `context.md` — sitting in the run folder I pointed at my Posts folder. Point it at a folder, and it sorts the files. One quick note before you save this: a readme is a short text file that explains what a tool is and how to use it. It is named 'readme' so that anyone who opens the folder reads it first. This description will become that file. Here is a first draft — change anything you want. It is yours."

User: "That is accurate. I want to add that nothing moves until I confirm the plan."

Coach: "That sounds like you."

_Draft (accepted with edit):_ "My Posts folder was a mess of images and videos, so I built a tool to fix that. It sorts any folder of media files into separate images and videos folders, naming each file by the date it was made — and nothing moves until I confirm the plan. The tool is two markdown files sitting in the run folder I pointed at my Posts folder. Point it at a folder, confirm, and it sorts the files."

---

## Save Step: Make It a Package

Coach: "Save that description as `readme.md` in your project folder — the same folder that has `identity.md` and `context.md`."
User: "Done."
Coach: "That folder is now a shareable package — the tool plus a description of what it does and how to use it."

---

**Great job!**
